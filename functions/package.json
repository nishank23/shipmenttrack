{
  "name": "shiptrack-functions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "lint": "node --check index.js",
    "serve": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "axios": "^1.13.0",
    "cheerio": "^1.1.2",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.2.5",
    "nodemailer": "^8.0.5"
  }
}
